/**
 * src/core/index.js
 *
 * Core module for Smart Maps OS (SM-IN-AM).
 * Responsible for bootstrapping the application, managing the lifecycle of all
 * subsystems, and exposing shared system services (config, events, logging).
 */
