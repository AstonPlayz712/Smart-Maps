# `npx cap sync ios` (and Appflow's build step) copies the Vite `dist/` build
# into this directory. This file just keeps the folder present in git so the
# Xcode project reference resolves on a fresh clone before the first sync.
